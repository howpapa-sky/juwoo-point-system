# 📋 주우 놀이시스템 - e북 퀴즈 연동 시스템 개발 의뢰서

**작성일**: 2025년 1월 17일  
**프로젝트**: 주우 놀이시스템 (juwoo-point-system)  
**버전**: v2.0 - e북 퀴즈 연동 업데이트

---

## 📌 1. 개요

### 1.1 배경
현재 시스템의 문제점:
1. **e북과 퀴즈의 연관성 부족**: 책을 읽지 않아도 퀴즈를 풀 수 있어 학습 효과 저하
2. **어려운 문제에서 포기 패턴**: 8세 아동(주우)이 어려운 문제를 만나면 아무거나 찍는 습관 발생
3. **성취감 부재**: 단계별 성취 시스템이 없어 동기부여 부족

### 1.2 목표
- e북 완독 후에만 해당 퀴즈에 도전할 수 있도록 **잠금 해제 시스템** 구현
- 틀려도 좌절하지 않고 **작은 성취부터** 쌓아가는 단계별 학습 시스템 구현
- 힌트 사용 시 포인트 감소로 **자기 힘으로 풀기** 유도

---

## 📌 2. 시스템 아키텍처

### 2.1 전체 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                        사용자 여정                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📚 e북 도서관                                                    │
│      │                                                           │
│      ▼                                                           │
│  📖 e북 읽기 시작                                                  │
│      │                                                           │
│      ▼                                                           │
│  ✅ e북 완독 (마지막 페이지 도달)                                    │
│      │                                                           │
│      ├──→ 🔓 해당 e북 전용 퀴즈 잠금 해제                           │
│      │                                                           │
│      ▼                                                           │
│  🎮 퀴즈 도전 버튼 활성화                                           │
│      │                                                           │
│      ▼                                                           │
│  ┌─────────────────────────────────────────┐                     │
│  │         📝 e북 전용 퀴즈 페이지           │                     │
│  │                                         │                     │
│  │  🌱 기초 퀴즈 (잠금 해제됨)               │                     │
│  │      │                                  │                     │
│  │      ▼ (기초 통과 시)                    │                     │
│  │  ⭐ 실력 퀴즈 (잠금 해제)                 │                     │
│  │      │                                  │                     │
│  │      ▼ (실력 통과 시)                    │                     │
│  │  🏆 마스터 퀴즈 (잠금 해제)               │                     │
│  │                                         │                     │
│  └─────────────────────────────────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 힌트 & 포인트 시스템

```
┌─────────────────────────────────────────────────────────────────┐
│                     힌트 사용에 따른 포인트                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  기본 포인트: 10점 (예시)                                          │
│                                                                  │
│  ┌─────────────────────────────────────────────────┐             │
│  │  힌트 0개 사용  →  10점 (100%)                   │             │
│  │  힌트 1개 사용  →   9점 (90%)                    │             │
│  │  힌트 2개 사용  →   8점 (80%)                    │             │
│  │  힌트 3개 사용  →   7점 (70%)                    │             │
│  │  ...                                            │             │
│  │  힌트 9개 사용  →   1점 (10%)                    │             │
│  │  힌트 10개 사용 →   1점 (최소 보장)               │             │
│  └─────────────────────────────────────────────────┘             │
│                                                                  │
│  💡 힌트 종류:                                                    │
│  1. 일반 힌트 (기존): 문제에 대한 힌트 텍스트                        │
│  2. 📖 책 힌트 (신규): "3페이지에서 찾아봐!" → 해당 페이지로 이동      │
│  3. 🎯 정답 힌트 (신규): 4지선다 중 오답 1개 제거                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 오답 시 격려 시스템

```
┌─────────────────────────────────────────────────────────────────┐
│                      오답 처리 플로우                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ❌ 오답 선택                                                     │
│      │                                                           │
│      ▼                                                           │
│  ┌─────────────────────────────────────────────────┐             │
│  │  "아쉬워! 하지만 괜찮아! 💪"                      │             │
│  │                                                 │             │
│  │  정답: [정답 표시]                               │             │
│  │  해설: [해설 표시]                               │             │
│  │                                                 │             │
│  │  📖 [e북에서 확인하기] 버튼                      │             │
│  │      → 클릭 시 해당 e북 페이지로 이동             │             │
│  │                                                 │             │
│  └─────────────────────────────────────────────────┘             │
│      │                                                           │
│      ▼                                                           │
│  연속 2회 오답 시:                                                 │
│  ┌─────────────────────────────────────────────────┐             │
│  │  "힌트 모드로 바꿀까? 힌트를 보면 더 쉬워져!"      │             │
│  │                                                 │             │
│  │  [힌트 모드로 변경] [계속 도전하기]               │             │
│  └─────────────────────────────────────────────────┘             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📌 3. 데이터베이스 스키마

### 3.1 신규 테이블

#### 3.1.1 `ebook_progress` - e북 읽기 진행률

```sql
CREATE TABLE IF NOT EXISTS ebook_progress (
  id SERIAL PRIMARY KEY,
  juwoo_id INTEGER DEFAULT 1 NOT NULL,
  book_id VARCHAR(100) NOT NULL,           -- booksData.ts의 book.id
  current_page INTEGER DEFAULT 0 NOT NULL, -- 현재 읽은 페이지 (0부터 시작)
  total_pages INTEGER NOT NULL,            -- 전체 페이지 수
  is_completed BOOLEAN DEFAULT false NOT NULL, -- 완독 여부
  completed_at TIMESTAMP,                  -- 완독 시간
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
  
  UNIQUE(juwoo_id, book_id)
);

-- 인덱스
CREATE INDEX idx_ebook_progress_book_id ON ebook_progress(book_id);
CREATE INDEX idx_ebook_progress_completed ON ebook_progress(is_completed);
```

#### 3.1.2 `ebook_quiz_progress` - e북별 퀴즈 진행률

```sql
CREATE TABLE IF NOT EXISTS ebook_quiz_progress (
  id SERIAL PRIMARY KEY,
  juwoo_id INTEGER DEFAULT 1 NOT NULL,
  book_id VARCHAR(100) NOT NULL,           -- 연결된 e북 ID
  quiz_tier VARCHAR(20) NOT NULL,          -- 'basic' | 'intermediate' | 'master'
  is_unlocked BOOLEAN DEFAULT false NOT NULL, -- 잠금 해제 여부
  is_completed BOOLEAN DEFAULT false NOT NULL, -- 완료 여부
  best_score INTEGER DEFAULT 0 NOT NULL,   -- 최고 점수
  total_attempts INTEGER DEFAULT 0 NOT NULL, -- 총 도전 횟수
  last_attempt_at TIMESTAMP,
  completed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
  
  UNIQUE(juwoo_id, book_id, quiz_tier)
);

-- 인덱스
CREATE INDEX idx_ebook_quiz_progress_book_id ON ebook_quiz_progress(book_id);
```

#### 3.1.3 `quiz_attempt_history` - 퀴즈 시도 기록

```sql
CREATE TABLE IF NOT EXISTS quiz_attempt_history (
  id SERIAL PRIMARY KEY,
  juwoo_id INTEGER DEFAULT 1 NOT NULL,
  book_id VARCHAR(100) NOT NULL,
  quiz_tier VARCHAR(20) NOT NULL,
  question_id VARCHAR(50) NOT NULL,        -- 문제 ID
  user_answer VARCHAR(500),                -- 사용자 답변
  is_correct BOOLEAN NOT NULL,
  hints_used INTEGER DEFAULT 0 NOT NULL,   -- 사용한 힌트 개수
  base_points INTEGER NOT NULL,            -- 기본 포인트
  earned_points INTEGER NOT NULL,          -- 실제 획득 포인트 (힌트 감소 적용)
  time_spent_seconds INTEGER,              -- 문제 풀이 시간
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

-- 인덱스
CREATE INDEX idx_quiz_attempt_history_book_id ON quiz_attempt_history(book_id);
CREATE INDEX idx_quiz_attempt_history_question_id ON quiz_attempt_history(question_id);
```

### 3.2 RLS 정책

```sql
-- ebook_progress RLS
ALTER TABLE ebook_progress ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow all for ebook_progress" ON ebook_progress FOR ALL USING (true);

-- ebook_quiz_progress RLS
ALTER TABLE ebook_quiz_progress ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow all for ebook_quiz_progress" ON ebook_quiz_progress FOR ALL USING (true);

-- quiz_attempt_history RLS
ALTER TABLE quiz_attempt_history ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow all for quiz_attempt_history" ON quiz_attempt_history FOR ALL USING (true);
```

---

## 📌 4. 데이터 구조 변경

### 4.1 `booksData.ts` 변경

```typescript
// 기존 Book 인터페이스 확장
export interface Book {
  id: string;
  title: string;
  author: string;
  coverEmoji: string;
  description: string;
  pages: string[];
  difficulty: "쉬움" | "보통" | "어려움";
  readTime: string;
  category: string;
  
  // 🆕 신규 필드
  hasQuiz: boolean;              // 전용 퀴즈 존재 여부
  quizRewardPoints: {            // 퀴즈 완료 시 보상 포인트
    basic: number;               // 기초 퀴즈 완료 보상
    intermediate: number;        // 실력 퀴즈 완료 보상
    master: number;              // 마스터 퀴즈 완료 보상
  };
}

// 예시
{
  id: "friend-system-guide",
  title: "친구 시스템 완전 정복!",
  // ... 기존 필드들 ...
  hasQuiz: true,
  quizRewardPoints: {
    basic: 50,
    intermediate: 100,
    master: 200,
  },
}
```

### 4.2 퀴즈 데이터 구조 변경

```typescript
// 기존 QuizQuestion 인터페이스 확장
export interface QuizQuestion {
  id: string;
  type: QuestionType;
  difficulty: Difficulty;
  category: string;
  question: string;
  image?: string;
  correctAnswer: string;
  acceptableAnswers?: string[];
  options?: string[];
  hint: string;
  explanation: string;
  points: number;
  
  // 🆕 신규 필드
  bookId: string;                // 연결된 e북 ID (필수)
  quizTier: "basic" | "intermediate" | "master"; // 퀴즈 단계
  pageReference: number;         // 정답이 있는 e북 페이지 번호 (1부터 시작)
  
  // 🆕 추가 힌트 옵션
  hints: {
    text: string;                // 일반 힌트 텍스트
    pageHint?: string;           // 책 페이지 힌트 (예: "3페이지에서 찾아봐!")
    eliminateOption?: number;    // 제거할 오답 인덱스 (0~3, 4지선다용)
  }[];
}

// 예시
{
  id: "fs1",
  type: "multiple-choice",
  difficulty: "easy",
  category: "친구 시스템",
  question: "포켓몬GO에서 친구를 추가할 때 필요한 것은?",
  correctAnswer: "트레이너 코드",
  options: ["트레이너 코드", "포켓코인", "별의모래", "몬스터볼"],
  points: 10,
  
  // 🆕 신규 필드
  bookId: "friend-system-guide",
  quizTier: "basic",
  pageReference: 2,              // 2페이지에 정답이 있음
  hints: [
    { text: "12자리 숫자로 된 나만의 특별한 번호예요!" },
    { pageHint: "📖 2페이지에서 찾아봐!", eliminateOption: 3 },
    { eliminateOption: 1 },
  ],
  
  hint: "12자리 숫자로 된 나만의 특별한 번호예요!", // 기존 호환용
  explanation: "트레이너 코드는 12자리 숫자로 된 고유 번호예요!",
}
```

### 4.3 퀴즈 데이터 파일 분리 (권장)

현재 `PokemonQuiz.tsx` 안에 퀴즈 데이터가 있어 관리가 어려움.  
별도 파일로 분리 권장:

```
client/src/data/
├── booksData.ts           # e북 데이터
├── quizData/
│   ├── index.ts           # 전체 퀴즈 export
│   ├── types.ts           # QuizQuestion 타입 정의
│   ├── friendSystemQuiz.ts    # 친구 시스템 퀴즈
│   ├── goBattleQuiz.ts        # GO 배틀 리그 퀴즈
│   ├── eventGuideQuiz.ts      # 이벤트 가이드 퀴즈
│   ├── rocketGuideQuiz.ts     # 로켓단 가이드 퀴즈
│   └── legacyQuiz.ts          # 기존 퀴즈 (하위 호환)
```

---

## 📌 5. 컴포넌트 설계

### 5.1 신규 페이지

#### 5.1.1 `/ebook-quiz/:bookId` - e북 전용 퀴즈 페이지

**파일 위치**: `client/src/pages/EbookQuiz.tsx`

```typescript
interface EbookQuizPageProps {
  bookId: string;  // URL 파라미터
}

// 페이지 구조
const EbookQuizPage = ({ bookId }: EbookQuizPageProps) => {
  // 1. 해당 e북 완독 여부 확인
  // 2. 완독하지 않았으면 잠금 화면 표시
  // 3. 완독했으면 퀴즈 단계 선택 화면 표시
  // 4. 퀴즈 진행
}
```

**화면 구성**:

```
┌─────────────────────────────────────────────────────────────┐
│  ← 뒤로    📖 친구 시스템 완전 정복! 퀴즈                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🌱 기초 퀴즈                                        │   │
│  │  "e북 내용을 잘 읽었는지 확인해봐!"                    │   │
│  │                                                     │   │
│  │  문제 5개 | 최고 점수: 45점                          │   │
│  │                                                     │   │
│  │  [🎮 도전하기]                    ✅ 완료!           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ⭐ 실력 퀴즈                                        │   │
│  │  "조금 더 생각이 필요한 문제들!"                       │   │
│  │                                                     │   │
│  │  문제 5개 | 최고 점수: --                            │   │
│  │                                                     │   │
│  │  [🎮 도전하기]                    🔒 기초 퀴즈 먼저!  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🏆 마스터 퀴즈                                       │   │
│  │  "진정한 마스터를 위한 도전!"                          │   │
│  │                                                     │   │
│  │  문제 5개 | 최고 점수: --                            │   │
│  │                                                     │   │
│  │  [🔒 잠김]                       🔒 실력 퀴즈 먼저!   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 5.1.2 잠금 화면 (e북 미완독 시)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                         🔒                                  │
│                                                             │
│              아직 책을 다 읽지 않았어!                         │
│                                                             │
│        📚 "친구 시스템 완전 정복!"을 먼저 읽어봐!              │
│                                                             │
│               진행률: ████████░░ 80%                        │
│               (12/15 페이지)                                 │
│                                                             │
│              [📖 이어서 읽기]                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 기존 컴포넌트 수정

#### 5.2.1 `EbookReader.tsx` 수정사항

**변경 1**: 페이지 진행률 저장

```typescript
// 페이지 이동 시 Supabase에 진행률 저장
const handlePageChange = async (newPage: number) => {
  setCurrentPage(newPage);
  
  await supabase.from('ebook_progress').upsert({
    juwoo_id: 1,
    book_id: book.id,
    current_page: newPage,
    total_pages: book.pages.length,
    is_completed: newPage === book.pages.length - 1,
    completed_at: newPage === book.pages.length - 1 ? new Date().toISOString() : null,
    updated_at: new Date().toISOString(),
  }, {
    onConflict: 'juwoo_id,book_id'
  });
};
```

**변경 2**: 마지막 페이지에 퀴즈 도전 버튼 추가

```typescript
// 마지막 페이지일 때
{isLastPage && book.hasQuiz && (
  <div className="mt-8 p-6 bg-gradient-to-r from-amber-100 to-orange-100 rounded-2xl">
    <h3 className="text-xl font-bold mb-2">🎉 책을 다 읽었어!</h3>
    <p className="text-gray-600 mb-4">이제 퀴즈에 도전해볼까?</p>
    <Link href={`/ebook-quiz/${book.id}`}>
      <Button className="w-full bg-gradient-to-r from-amber-500 to-orange-500">
        🎮 퀴즈 도전하기!
      </Button>
    </Link>
  </div>
)}
```

**변경 3**: 이어서 읽기 기능

```typescript
// 컴포넌트 마운트 시 저장된 페이지에서 시작
useEffect(() => {
  const loadProgress = async () => {
    const { data } = await supabase
      .from('ebook_progress')
      .select('current_page')
      .eq('juwoo_id', 1)
      .eq('book_id', book.id)
      .single();
    
    if (data?.current_page) {
      setCurrentPage(data.current_page);
    }
  };
  loadProgress();
}, [book.id]);
```

#### 5.2.2 `EbookLibrary.tsx` 수정사항

**변경**: 각 책에 진행률 & 퀴즈 상태 표시

```typescript
// 책 카드에 추가할 정보
<div className="flex items-center gap-2 mt-2">
  {/* 읽기 진행률 */}
  {progress && (
    <span className="text-xs bg-blue-100 text-blue-700 px-2 py-1 rounded-full">
      📖 {progress.is_completed ? '완독!' : `${Math.round((progress.current_page / progress.total_pages) * 100)}%`}
    </span>
  )}
  
  {/* 퀴즈 상태 */}
  {book.hasQuiz && (
    <span className={`text-xs px-2 py-1 rounded-full ${
      quizProgress?.is_completed 
        ? 'bg-green-100 text-green-700' 
        : progress?.is_completed 
          ? 'bg-amber-100 text-amber-700'
          : 'bg-gray-100 text-gray-500'
    }`}>
      {quizProgress?.is_completed ? '🏆 퀴즈 완료' : 
       progress?.is_completed ? '🎮 퀴즈 가능' : '🔒 퀴즈 잠김'}
    </span>
  )}
</div>
```

### 5.3 신규 컴포넌트

#### 5.3.1 `QuizQuestion.tsx` - 문제 풀이 컴포넌트

```typescript
interface QuizQuestionProps {
  question: QuizQuestion;
  onAnswer: (answer: string, hintsUsed: number, isCorrect: boolean) => void;
  onViewBook: (pageNumber: number) => void;  // 책 페이지로 이동
  bookTitle: string;
}

// 상태 관리
const [hintsUsed, setHintsUsed] = useState<number[]>([]); // 사용한 힌트 인덱스
const [showHint, setShowHint] = useState(false);
const [eliminatedOptions, setEliminatedOptions] = useState<number[]>([]);
const [isAnswered, setIsAnswered] = useState(false);
const [consecutiveWrong, setConsecutiveWrong] = useState(0);
```

**힌트 사용 UI**:

```
┌─────────────────────────────────────────────────────────────┐
│  💡 힌트 (3개 남음)                     포인트: 10 → 8점     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [💬 힌트 1] [📖 책에서 찾기] [❌ 오답 제거]                  │
│                                                             │
│  * 힌트 1개당 포인트 10% 감소                                │
│  * 최대 9개까지 사용 가능 (최소 1점 보장)                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 5.3.2 `QuizResult.tsx` - 퀴즈 결과 컴포넌트

```typescript
interface QuizResultProps {
  totalQuestions: number;
  correctCount: number;
  totalScore: number;
  maxPossibleScore: number;
  hintsUsed: number;
  timeSpent: number;
  isPassed: boolean;              // 통과 여부 (60% 이상)
  nextTierUnlocked: boolean;      // 다음 단계 해금 여부
  bookId: string;
  quizTier: string;
}
```

**결과 화면 구성**:

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    🎉 잘했어, 주우!                          │
│                                                             │
│              ⭐⭐⭐⭐⭐ (5/5 정답)                           │
│                                                             │
│  ┌───────────────────────────────────────────────────┐     │
│  │  획득 포인트         │        42점                 │     │
│  │  힌트 사용           │        3개 (-30%)           │     │
│  │  걸린 시간           │        2분 30초             │     │
│  └───────────────────────────────────────────────────┘     │
│                                                             │
│           🔓 ⭐ 실력 퀴즈가 열렸어!                          │
│                                                             │
│  [🏠 도서관으로]  [🔄 다시 도전]  [⭐ 실력 퀴즈 도전]         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**실패 시 격려 화면**:

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    💪 괜찮아, 다시 해보자!                    │
│                                                             │
│              ⭐⭐☆☆☆ (2/5 정답)                            │
│                                                             │
│           60% 이상 맞아야 통과야 (현재 40%)                   │
│                                                             │
│  ┌───────────────────────────────────────────────────┐     │
│  │  💡 틀린 문제들은 책에서 다시 확인해봐!             │     │
│  │                                                   │     │
│  │  • 문제 2: 📖 3페이지에 답이 있어!                 │     │
│  │  • 문제 4: 📖 7페이지에 답이 있어!                 │     │
│  │  • 문제 5: 📖 11페이지에 답이 있어!                │     │
│  └───────────────────────────────────────────────────┘     │
│                                                             │
│  [📖 책 다시 읽기]           [🔄 다시 도전]                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📌 6. 포인트 계산 로직

### 6.1 힌트 사용에 따른 포인트 계산

```typescript
/**
 * 힌트 사용에 따른 최종 포인트 계산
 * @param basePoints - 기본 포인트
 * @param hintsUsed - 사용한 힌트 개수
 * @returns 최종 획득 포인트
 */
const calculateFinalPoints = (basePoints: number, hintsUsed: number): number => {
  // 힌트 1개당 10% 감소
  const reduction = hintsUsed * 0.1;
  
  // 최대 90% 감소 (최소 10% = 1점 이상 보장)
  const finalMultiplier = Math.max(0.1, 1 - reduction);
  
  // 반올림하여 정수로 반환
  return Math.round(basePoints * finalMultiplier);
};

// 예시
calculateFinalPoints(10, 0);  // 10점 (100%)
calculateFinalPoints(10, 1);  // 9점 (90%)
calculateFinalPoints(10, 2);  // 8점 (80%)
calculateFinalPoints(10, 5);  // 5점 (50%)
calculateFinalPoints(10, 9);  // 1점 (10%)
calculateFinalPoints(10, 10); // 1점 (최소 보장)
calculateFinalPoints(10, 15); // 1점 (최소 보장)
```

### 6.2 퀴즈 통과 조건

```typescript
/**
 * 퀴즈 통과 여부 확인
 * @param correctCount - 정답 개수
 * @param totalQuestions - 전체 문제 수
 * @returns 통과 여부
 */
const isQuizPassed = (correctCount: number, totalQuestions: number): boolean => {
  const percentage = (correctCount / totalQuestions) * 100;
  return percentage >= 60;  // 60% 이상 정답 시 통과
};
```

### 6.3 단계 해금 조건

```typescript
/**
 * 다음 퀴즈 단계 해금 조건
 */
const UNLOCK_CONDITIONS = {
  basic: {
    requirement: 'e북 완독',
    check: (ebookProgress: EbookProgress) => ebookProgress.is_completed,
  },
  intermediate: {
    requirement: '기초 퀴즈 60% 이상 통과',
    check: (quizProgress: QuizProgress) => 
      quizProgress.quiz_tier === 'basic' && quizProgress.is_completed,
  },
  master: {
    requirement: '실력 퀴즈 60% 이상 통과',
    check: (quizProgress: QuizProgress) => 
      quizProgress.quiz_tier === 'intermediate' && quizProgress.is_completed,
  },
};
```

---

## 📌 7. 라우팅 설정

### 7.1 App.tsx 라우트 추가

```typescript
import EbookQuiz from "@/pages/EbookQuiz";

// 라우트 추가
<Route path="/ebook-quiz/:bookId" component={EbookQuiz} />
```

### 7.2 전체 라우트 구조

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/ebook-library` | EbookLibrary | e북 도서관 (목록) |
| `/ebook-reader/:bookId` | EbookReader | e북 리더 (읽기) |
| `/ebook-quiz/:bookId` | EbookQuiz | e북 전용 퀴즈 |
| `/pokemon-quiz` | PokemonQuiz | 기존 종합 퀴즈 (하위 호환) |

---

## 📌 8. API 엔드포인트 (Supabase 직접 호출)

### 8.1 e북 진행률 관련

```typescript
// 진행률 조회
const getEbookProgress = async (bookId: string) => {
  return await supabase
    .from('ebook_progress')
    .select('*')
    .eq('juwoo_id', 1)
    .eq('book_id', bookId)
    .single();
};

// 진행률 저장/업데이트
const saveEbookProgress = async (bookId: string, currentPage: number, totalPages: number) => {
  const isCompleted = currentPage >= totalPages - 1;
  
  return await supabase
    .from('ebook_progress')
    .upsert({
      juwoo_id: 1,
      book_id: bookId,
      current_page: currentPage,
      total_pages: totalPages,
      is_completed: isCompleted,
      completed_at: isCompleted ? new Date().toISOString() : null,
      updated_at: new Date().toISOString(),
    }, {
      onConflict: 'juwoo_id,book_id'
    });
};
```

### 8.2 퀴즈 진행률 관련

```typescript
// 퀴즈 진행률 조회
const getQuizProgress = async (bookId: string) => {
  return await supabase
    .from('ebook_quiz_progress')
    .select('*')
    .eq('juwoo_id', 1)
    .eq('book_id', bookId)
    .order('quiz_tier');
};

// 퀴즈 완료 처리
const completeQuiz = async (
  bookId: string, 
  quizTier: string, 
  score: number
) => {
  // 1. 퀴즈 진행률 업데이트
  await supabase
    .from('ebook_quiz_progress')
    .upsert({
      juwoo_id: 1,
      book_id: bookId,
      quiz_tier: quizTier,
      is_completed: true,
      best_score: score,
      total_attempts: supabase.sql`total_attempts + 1`,
      completed_at: new Date().toISOString(),
      updated_at: new Date().toISOString(),
    }, {
      onConflict: 'juwoo_id,book_id,quiz_tier'
    });
  
  // 2. 다음 단계 해금
  const nextTier = quizTier === 'basic' ? 'intermediate' : 
                   quizTier === 'intermediate' ? 'master' : null;
  
  if (nextTier) {
    await supabase
      .from('ebook_quiz_progress')
      .upsert({
        juwoo_id: 1,
        book_id: bookId,
        quiz_tier: nextTier,
        is_unlocked: true,
        updated_at: new Date().toISOString(),
      }, {
        onConflict: 'juwoo_id,book_id,quiz_tier'
      });
  }
  
  // 3. 포인트 지급
  await supabase.from('point_transactions').insert({
    juwoo_id: 1,
    rule_id: null,
    amount: score,
    note: `📚 ${bookId} - ${quizTier} 퀴즈 완료`,
    created_by: 1,
  });
  
  // 4. juwoo_profile 포인트 업데이트
  await supabase.rpc('increment_points', { points_to_add: score });
};
```

### 8.3 포인트 증가 RPC 함수

```sql
-- Supabase에 추가할 RPC 함수
CREATE OR REPLACE FUNCTION increment_points(points_to_add INTEGER)
RETURNS void AS $$
BEGIN
  UPDATE juwoo_profile 
  SET current_points = current_points + points_to_add,
      updated_at = NOW()
  WHERE id = 1;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

---

## 📌 9. 퀴즈 데이터 마이그레이션

### 9.1 기존 퀴즈 데이터 변환

기존 `PokemonQuiz.tsx`의 `allQuizData`를 새 형식으로 변환:

```typescript
// 변환 전 (기존)
{
  id: "fs1",
  type: "multiple-choice",
  difficulty: "easy",
  category: "친구 시스템",
  question: "포켓몬GO에서 친구를 추가할 때 필요한 것은?",
  correctAnswer: "트레이너 코드",
  options: ["트레이너 코드", "포켓코인", "별의모래", "몬스터볼"],
  hint: "12자리 숫자로 된 나만의 특별한 번호예요!",
  explanation: "트레이너 코드는 12자리 숫자로 된 고유 번호예요!",
  points: 5,
}

// 변환 후 (신규)
{
  id: "fs1",
  type: "multiple-choice",
  difficulty: "easy",
  category: "친구 시스템",
  question: "포켓몬GO에서 친구를 추가할 때 필요한 것은?",
  correctAnswer: "트레이너 코드",
  options: ["트레이너 코드", "포켓코인", "별의모래", "몬스터볼"],
  explanation: "트레이너 코드는 12자리 숫자로 된 고유 번호예요!",
  points: 10,                              // 🆕 기본 10점으로 통일
  
  // 🆕 신규 필드
  bookId: "friend-system-guide",
  quizTier: "basic",
  pageReference: 2,
  hints: [
    { text: "12자리 숫자로 된 나만의 특별한 번호예요!" },
    { pageHint: "📖 2페이지 '친구를 추가하는 방법'에서 찾아봐!" },
    { eliminateOption: 3 },  // "몬스터볼" 제거
  ],
  
  // 🆕 하위 호환용 (deprecated, 추후 제거)
  hint: "12자리 숫자로 된 나만의 특별한 번호예요!",
}
```

### 9.2 퀴즈 난이도 → 단계 매핑

| 기존 difficulty | 신규 quizTier | 기본 포인트 |
|----------------|---------------|------------|
| easy | basic | 10점 |
| medium | intermediate | 15점 |
| hard | master | 20점 |

---

## 📌 10. UI/UX 가이드라인

### 10.1 색상 테마

| 요소 | 색상 | 용도 |
|------|------|------|
| 🌱 기초 | `green-500` (#22c55e) | 기초 퀴즈 관련 |
| ⭐ 실력 | `amber-500` (#f59e0b) | 실력 퀴즈 관련 |
| 🏆 마스터 | `purple-500` (#a855f7) | 마스터 퀴즈 관련 |
| 🔒 잠김 | `gray-400` (#9ca3af) | 잠긴 콘텐츠 |
| ✅ 정답 | `emerald-500` (#10b981) | 정답 표시 |
| ❌ 오답 | `rose-500` (#f43f5e) | 오답 표시 |
| 💡 힌트 | `sky-500` (#0ea5e9) | 힌트 관련 |

### 10.2 애니메이션

```typescript
// 정답 시 축하 효과 (기존 confetti 활용)
import confetti from "canvas-confetti";

const celebrateCorrect = () => {
  confetti({
    particleCount: 50,
    spread: 60,
    origin: { y: 0.7 },
    colors: ['#22c55e', '#f59e0b', '#a855f7'],
  });
};
```

### 10.3 격려 메시지 목록

```typescript
const ENCOURAGEMENT_MESSAGES = {
  correct: [
    "🎉 대단해! 정답이야!",
    "⭐ 와! 똑똑한데?",
    "🚀 멋져! 계속 가보자!",
    "💪 역시 주우야!",
    "🌟 완벽해!",
  ],
  wrong: [
    "💪 괜찮아! 다시 해보자!",
    "🤔 아쉬워! 다음엔 맞출 수 있어!",
    "📖 책에서 다시 확인해볼까?",
    "🌱 실수해도 괜찮아, 배우는 거야!",
    "💡 힌트를 볼까?",
  ],
  streak: [
    "🔥 2연속 정답!",
    "🔥🔥 3연속! 대단해!",
    "🔥🔥🔥 4연속!! 천재야?",
    "🌟🌟🌟 5연속!!! 완벽해!!!",
  ],
};
```

---

## 📌 11. 테스트 시나리오

### 11.1 e북 완독 → 퀴즈 해금 테스트

```
1. 새 사용자로 로그인
2. e북 도서관에서 "친구 시스템 완전 정복!" 선택
3. 책의 1페이지부터 마지막 페이지까지 읽기
4. 마지막 페이지에서 "퀴즈 도전하기" 버튼 확인
5. 퀴즈 페이지로 이동
6. 기초 퀴즈가 해금되어 있는지 확인
7. 실력/마스터 퀴즈가 잠겨있는지 확인
```

### 11.2 힌트 사용 포인트 감소 테스트

```
1. 기초 퀴즈 시작 (기본 10점 문제)
2. 힌트 0개 사용 후 정답 → 10점 획득 확인
3. 다음 문제에서 힌트 1개 사용 후 정답 → 9점 획득 확인
4. 다음 문제에서 힌트 3개 사용 후 정답 → 7점 획득 확인
5. 결과 화면에서 총 점수 확인 (10 + 9 + 7 = 26점)
```

### 11.3 오답 시 격려 시스템 테스트

```
1. 퀴즈 진행 중 일부러 오답 선택
2. 격려 메시지 표시 확인
3. "e북에서 확인하기" 버튼으로 해당 페이지 이동 확인
4. 연속 2회 오답 시 "힌트 모드 변경" 제안 확인
```

### 11.4 단계 해금 테스트

```
1. 기초 퀴즈 60% 미만으로 완료 → 실력 퀴즈 잠김 유지 확인
2. 기초 퀴즈 60% 이상으로 완료 → 실력 퀴즈 해금 확인
3. 실력 퀴즈 60% 이상으로 완료 → 마스터 퀴즈 해금 확인
```

---

## 📌 12. 파일 변경 목록

### 12.1 신규 파일

| 파일 경로 | 설명 |
|----------|------|
| `client/src/pages/EbookQuiz.tsx` | e북 전용 퀴즈 페이지 |
| `client/src/components/quiz/QuizQuestion.tsx` | 문제 풀이 컴포넌트 |
| `client/src/components/quiz/QuizResult.tsx` | 결과 화면 컴포넌트 |
| `client/src/components/quiz/HintButton.tsx` | 힌트 버튼 컴포넌트 |
| `client/src/data/quizData/index.ts` | 퀴즈 데이터 통합 |
| `client/src/data/quizData/types.ts` | 퀴즈 타입 정의 |
| `client/src/data/quizData/friendSystemQuiz.ts` | 친구 시스템 퀴즈 |
| `client/src/hooks/useEbookProgress.ts` | e북 진행률 훅 |
| `client/src/hooks/useQuizProgress.ts` | 퀴즈 진행률 훅 |

### 12.2 수정 파일

| 파일 경로 | 변경 내용 |
|----------|----------|
| `client/src/App.tsx` | 라우트 추가 |
| `client/src/data/booksData.ts` | Book 인터페이스 확장 |
| `client/src/pages/EbookReader.tsx` | 진행률 저장, 퀴즈 버튼 추가 |
| `client/src/pages/EbookLibrary.tsx` | 진행률/퀴즈 상태 표시 |
| `client/src/pages/PokemonQuiz.tsx` | 기존 퀴즈 하위 호환 유지 |
| `CLAUDE.md` | 시스템 문서 업데이트 |

### 12.3 데이터베이스 변경

| 테이블/함수 | 작업 |
|------------|------|
| `ebook_progress` | 신규 테이블 생성 |
| `ebook_quiz_progress` | 신규 테이블 생성 |
| `quiz_attempt_history` | 신규 테이블 생성 |
| `increment_points()` | 신규 RPC 함수 생성 |

---

## 📌 13. 개발 우선순위

### Phase 1: 기반 구축 (필수)
1. ✅ 데이터베이스 테이블 생성
2. ✅ 퀴즈 타입 정의 및 데이터 구조 변경
3. ✅ `EbookReader.tsx` 진행률 저장 기능

### Phase 2: 핵심 기능 (필수)
4. ✅ `EbookQuiz.tsx` 페이지 개발
5. ✅ 힌트 시스템 및 포인트 계산 로직
6. ✅ 단계별 잠금 해제 시스템

### Phase 3: UX 개선 (권장)
7. ✅ 격려 메시지 및 애니메이션
8. ✅ `EbookLibrary.tsx` 상태 표시
9. ✅ 오답 시 책 페이지 이동 기능

### Phase 4: 데이터 마이그레이션 (권장)
10. ✅ 기존 퀴즈 데이터 새 형식으로 변환
11. ✅ 퀴즈 데이터 파일 분리

---

## 📌 14. 체크리스트

### 개발 전 확인사항
- [ ] Supabase 대시보드에서 테이블 생성 권한 확인
- [ ] 기존 `PokemonQuiz.tsx` 백업
- [ ] 기존 `booksData.ts` 백업

### 개발 중 확인사항
- [ ] TypeScript 타입 오류 없음
- [ ] Supabase RLS 정책 적용됨
- [ ] 포인트 계산 로직 단위 테스트 통과
- [ ] wouter 라우팅 정상 작동

### 개발 후 확인사항
- [ ] 모든 테스트 시나리오 통과
- [ ] 모바일 반응형 UI 확인
- [ ] 콘솔 에러 없음
- [ ] Netlify 배포 성공

---

## 📌 15. 참고 자료

- **프로젝트 문서**: `CLAUDE.md`
- **기존 퀴즈 구현**: `client/src/pages/PokemonQuiz.tsx`
- **기존 e북 구현**: `client/src/pages/EbookReader.tsx`
- **UI 컴포넌트**: `client/src/components/ui/` (shadcn/ui)
- **Supabase 클라이언트**: `client/src/lib/supabaseClient.ts`

---

**문의사항이나 추가 요청사항이 있으면 언제든 말씀해주세요!**
